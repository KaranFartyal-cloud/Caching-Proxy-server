{
  "name": "caching-proxy",
  "version": "1.0.0",
  "description": "A CLI tool that starts a caching proxy server, forwarding requests to an origin server and caching the responses.",
  "main": "lib/server.js",
  "bin": {
    "caching-proxy": "./bin/caching-proxy.js"
  },
  "scripts": {
    "start": "node ./bin/caching-proxy.js",
    "dev" : "nodemon ./bin/caching-proxy.js"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^1.7.7",
    "commander": "^12.1.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
